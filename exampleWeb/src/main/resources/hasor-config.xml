<?xml version="1.0" encoding="UTF-8"?>
<config xmlns="http://project.hasor.net/hasor/schema/main">
    <!-- 框架配置 -->
    <hasor>
        <!-- 包扫描路径 -->
        <loadPackages>net.demo.hasor.*</loadPackages>
        <!-- 程序启动入口 -->
        <startup>net.demo.hasor.core.StartModule</startup>
        <!-- 工作目录 -->
        <environmentVar>
            <!-- 系统环境变量:EXAMPLE_HOME。在本示例项目中,该环境变量在 dockerfile 中进行的声明 -->
            <WORK_HOME>%EXAMPLE_HOME%</WORK_HOME>

            <EXAMPLE_HOME>%HASOR_WEBROOT%/WEB-INF/daily_home</EXAMPLE_HOME>
        </environmentVar>
    </hasor>

    <injectVars>
        <!-- 当前最新版本 -->
        <curentVersion>2.4.0</curentVersion>
        <!-- QQ登录接入,授权key -->
        <qqAdmins>${admins}</qqAdmins>
    </injectVars>

    <!-- 数据库链接: see -> 'env.config' -->
    <jdbcSettings>
        <jdbcDriver>${jdbc.driver}</jdbcDriver>
        <jdbcURL>${jdbc.url}</jdbcURL>
        <userName>${jdbc.user}</userName>
        <userPassword>${jdbc.password}</userPassword>
    </jdbcSettings>
</config>