
// 说明：数据类型测试。

var title = "性别：";

var fo = lambda: (obj) -> {
    if (obj >= 0)
        return title + "男";
    else
        return title + "女";
    end
}

var user = {
    "sex" : 1,
    "name": "zyc"
}

return fo



// 闭包特性
// 多维数组
// 各种路由表达式
