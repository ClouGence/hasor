<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:root="http://project.hasor.net/hasor/schema/main" xmlns="http://project.hasor.net/hasor/schema/hasor-rsf-runtime">
    <!-- 框架配置，包扫描范围 -->
    <hasor>
        <!-- 包扫描路径 -->
        <loadPackages>net.hasor.rsf.center.server.*</loadPackages>
        <!-- 程序启动入口 -->
        <startup>net.hasor.rsf.center.server.core.startup.RsfCenterServerModule</startup>
    </hasor>

    <!--
        RSF配置
            - enable  	   : 确保设置为true，Rsf与注册中心通信是通过RSF自身协议。
            - address 	   : RSF，无法在多个网卡上同时对外提供服务，因此您需要指定一个IP地址来告诉RSF工作在哪张网卡上。
                                “local”和“localhost”的区别，local指的是，本地默认网卡的IP地址。“localhost”对应的IP地址是“127.0.0.1”
                                如果您的部署环境有多张网卡，那么可以通过指定ip地址，来告诉RSF具体工作的网卡地址。
            - port    	   : RSF对外提供服务的监听端口号。
            - console.port : RSF管理控制台(Telnet)，监听端口号。
            - unitName     : 所属单元。
    -->
    <hasor.rsfConfig enable="true" address="127.0.0.1" port="2120" console.port="2121" unitName="default"/>
    <!--
        注册中心配置：
            - workAt  : 注册中心工作模式，可选的配置有：alone(单机模式，一台机器)、cluster(集群模式，基数台机器)
            
    -->
    <rsfCenter workAt="alone">
        <clusterServers>
            rsf://127.0.0.1:8001/unit
            rsf://127.0.0.1:8001/unit
            rsf://127.0.0.1:8001/unit
        </clusterServers>

        <!-- 服务推送配置 -->
        <push>
            <!-- 默认推送使用的：应用程序代码 -->
            <anonymousAppCode>anonymous</anonymousAppCode>
            <!-- 推送队列最大长度，当待推送服务达到这个阀值之后注册中心就会将新的事件交给集群其它机器去处理。 -->
            <queueMaxSize>10000</queueMaxSize>
            <!-- 当遇到推送队列满了之后等待多长时间重试一次，如果重试的时候队列依然满的，那么转发到其它机器上。单位毫秒 -->
            <sleepTime>1000</sleepTime>
        </push>
    </rsfCenter>


</config>