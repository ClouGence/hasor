更新日志:

v1.1.0 (2012-10)
* 修改引用关系，把noe-core-base-1.0.0 改成maven管理
* 修改针对action的处理，提供针对action的权限点查询，以辅助非需要登录的action的处理简化配置


v1.0.1 (2012-10-12)

* orm工具新增 批量更新方法
   public int[] updateSqlWithArgArray(String sql, List<Object[]> list);
    例如：    String sql="delete from table001 where tabkey=?";
         List<Object[]> arglist=new ArrayList<Object[]>();
          arglist.add(new Object[]{"key001"});
          arglist.add(new Object[]{"key002"});
		 AppUtil.getOrm().updateSqlWithArgArray(sql,arglist);

   public int[] updateSqlWithArrayMap(String sql, Map[] maps)

*   例如：    String sql="delete from table001 where tabkey=:tabkey";
          Map[] maps=new HashMap[2];
          maps[0]=new HashMap();
          maps[0].put("tabkey","key001");
          maps[1]=new HashMap();
          maps[1].put("tabkey","key002");
		 AppUtil.getOrm().updateSqlWithArrayMap(sql,maps);
*    增加针对map的批处理存储 
	  public void insertBatchMap(String tableName, List<Map<String, Object>> list) ;

*    修正public void insertObjList(String tableName, Collection<?> list); 避免因错误使用造成问题；	  	

v1.0.0 (2012-10-09)

* 系统功能定版。